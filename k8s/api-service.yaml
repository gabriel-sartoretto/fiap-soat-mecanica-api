apiVersion: v1
kind: Service
metadata:
  name: mecanica-api
  namespace: ${namespace}
spec:
  type: NodePort
  selector:
    app: mecanica-api
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: ${api_node_port}
      protocol: TCP
