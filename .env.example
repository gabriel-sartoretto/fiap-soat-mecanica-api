# Copie este arquivo para .env e substitua os placeholders.
# O arquivo .env local nao deve ser versionado.

POSTGRES_DB=mecanica
POSTGRES_USER=postgres
DB_PASSWORD=replace-with-a-strong-local-password
JWT_SECRET=replace-with-a-base64-encoded-secret
SONAR_DB_PASSWORD=replace-with-a-separate-local-password
